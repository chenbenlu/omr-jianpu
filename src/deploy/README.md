# `src.deploy` — Integration & Deployment (Owner: Member D)

End-to-end pipeline glue, Streamlit demo UI, and Docker / packaging concerns. Imports from `src.data`, `src.model`, and `src.postproc` to wire the full inference path.
