# `src.model` — Model Training (Owner: Member B)

Vision-Encoder-Decoder definition, loss functions, optimizer / scheduler setup, and the training loop live here. Consumes batches from `src.data`; emits checkpoints (gitignored) and Jianpu semantic-token logits.
