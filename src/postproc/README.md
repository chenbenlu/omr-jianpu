# `src.postproc` — Post-processing (Owner: Member C)

Translates the decoder's output semantic-token sequence into rendered Jianpu via the music-theory mapping rules. Pure-Python; no GPU dependency. Consumed by `src.deploy` for the end-user UI.
