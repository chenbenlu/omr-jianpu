# `src.data` — Data Engineering (Owner: Member A)

Camera-PrIMuS parsing, augmentation pipelines, and PyTorch `Dataset` / `DataLoader` implementations live here. Output schema is the contract consumed by `src.model`; coordinate any change via a cross-module PR.
