# OMR-to-Jianpu Python dependencies.
#
# IMPORTANT: Do NOT add torch / torchvision / torchaudio here.
# They are preinstalled in the base image (pytorch/pytorch:2.2.0-cuda12.1-cudnn8-devel)
# and re-pinning them via pip can silently resolve to a CPU-only wheel, disabling
# GPU acceleration on every team member's machine.

# --- Hugging Face stack (Vision-Encoder-Decoder backbone) ---
transformers>=4.40,<5.0
accelerate>=0.30
datasets>=2.19
evaluate>=0.4
tokenizers>=0.19
safetensors>=0.4

# --- Vision / augmentation (Member A) ---
opencv-python-headless>=4.9
pillow>=10.2
albumentations>=1.4
einops>=0.7

# --- Music utilities (Member C: Jianpu mapping) ---
music21>=9.1

# --- Training infrastructure (Member B) ---
tensorboard>=2.16
wandb>=0.17
tqdm>=4.66
pyyaml>=6.0
hydra-core>=1.3

# --- Notebook / UI (Member D) ---
jupyterlab>=4.1
ipywidgets>=8.1
streamlit>=1.34
matplotlib>=3.8

# --- Numerics ---
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4

# --- Dev tooling ---
ruff>=0.4
black>=24.4
pytest>=8.2
pre-commit>=3.7
