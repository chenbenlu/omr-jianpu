# Screenshots referenced by the project README

Drop captures here so the main `README.md` can link them without going stale.
Expected filenames (the main README links to these — keep the names stable):

- `space-upload.png` — the Space's *Upload image* tab, showing an input
  staff PNG (left) and the model's pretty-Jianpu output (right).
- `space-sample.png` — the *Val sample* tab on sample index 0, with the
  ground-truth Jianpu shown below the prediction for a visible match check.
- `space-engraved.png` — the *Engraved* notation view, with verovio's
  re-engraved staff rendering of the model's prediction.

Capture at 1280 × ~800 (browser default zoom) so they render at sensible size
in GitHub's markdown viewer.
